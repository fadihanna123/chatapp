{
  "name": "server",
  "version": "1.0.22",
  "lastUpgrade": "20251029",
  "scripts": {
    "setup": "yarn --emoji --silent --ignore-scripts --immutable",
    "start": "node dist/server",
    "dev": "ts-node-dev --poll -r tsconfig-paths/register src/server",
    "build": "esbuild ./src/server.ts --bundle --minify --sourcemap --outdir=./build --platform=node",
    "studio": "prisma studio",
    "lint": "eslint && tsc --noEmit && yarn type-check",
    "format": "prettier ./ -w",
    "seed": "prisma db push && prisma db pull && prisma generate",
    "type-check": "tsc --noEmit",
    "clear": "rimraf ./node_modules ./build"
  },
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "6.18.0",
    "dotenv": "17.2.3",
    "prisma": "6.18.0",
    "socket.io": "4.8.1"
  },
  "devDependencies": {
    "@eslint/js": "9.38.0",
    "esbuild": "^0.25.12",
    "eslint": "9.38.0",
    "globals": "16.4.0",
    "prettier": "3.6.2",
    "ts-node-dev": "2.0.0",
    "tsconfig-paths": "4.2.0",
    "typescript": "5.9.3",
    "typescript-eslint": "8.46.2"
  }
}
